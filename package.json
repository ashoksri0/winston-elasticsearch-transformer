{
  "name": "@restorecommerce/winston-elasticsearch-transformer",
  "version": "0.1.0",
  "description": "Transformer function for the winston-elasticsearch log transport",
  "main": "index",
  "author": "Invend GmbH",
  "repository": {
    "type": "git",
    "url": "https://github.com/restorecommerce/winston-elasticsearch-transformer.git"
  },
  "license": "MIT",
  "keywords": [
    "restore",
    "commerce",
    "log",
    "winston",
    "elasticsearch",
    "transformer"
  ],
  "dependencies": {},
  "devDependencies": {
    "@restorecommerce/eslint-config-restorecommerce": "^0.1.0",
    "coveralls": "^3.0.0",
    "is-generator": "^1.0.3",
    "istanbul": "^0.4.5",
    "mocha": "^4.0.1",
    "should": "^13.1.2"
  },
  "scripts": {
    "test": "npm run lint && npm run mocha",
    "lint": "eslint *.json *.js",
    "mocha": "mocha ./test/*",
    "coveralls": "istanbul cover ./node_modules/mocha/bin/_mocha --report lcovonly -- -R spec ./test/*"
  },
  "engines": {
    "node": ">= 8.0.0"
  }
}
